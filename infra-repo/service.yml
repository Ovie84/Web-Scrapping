apiVersion: v1
kind: Service
metadata:
  name: video-downloader-service
spec:
  selector:
    app: video-downloader
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer